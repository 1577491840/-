# 模板
```cpp
#include<bits/stdc++.h>
using namespace std;
#define ll long long
#define sc second
#define fr first
#define int long long
#define itn long long
#define vec vector
#define pii pair<int,int>
#define all(a) a.begin(),a.end()
#define ui unordered_map<int,int>
const int N = 5e5+5;
const int INF = INT_MAX/2;
// const int mod = 998244353;
// const int mod = 1e9+7;

void solve(){
    
}
signed main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    int _=1;
    //cin >> _;
    while(_--){
        solve();
    }
}
```
# 打表模板  
```cpp
#include <iostream>
#include <fstream>
using namespace std;

const int MAX_N = 1000;
int f[MAX_N + 1];

// 1. 先计算打表数据（不变）
void precompute() {
    f[0] = 0;
    f[1] = 1;
    for (int n = 2; n <= MAX_N; ++n) {
        f[n] = 1;
        for (int i = 1; i <= n / 2; ++i) {
            f[n] += f[i];
        }
    }
}

int main() {
    precompute();  // 先算好所有答案
    
    // 2. ofstream写文件（模板部分）
    ofstream out;
    out.open("C:\\Users\\你的用户名\\Desktop\\table.txt");
    if (!out.is_open()) {
        cout << "文件打开失败！" << endl;
        return 1;
    }

    // 3. 写数组格式（打表核心）
    out << "int ans[] = {";
    for (int n = 0; n <= MAX_N; ++n) {
        out << f[n];
        if (n != MAX_N) out << ", ";
    }
    out << "};" << endl;

    out.close();
    cout << "打表完成！去桌面找table.txt" << endl;
    return 0;
}
```
# 位运算
1. 按位与&
   **对应位都**为1时，结果才为1，否则为0。
2. 按位或（|)
   **对应位有1个为1**，结果就为1。
3. 按位异或（  ^  ) XOR
   **对应位不同**时结果为1，相同时为0,((   异或2次相同的相当于没有异或  ））
4. 按位取反( ~ )
   **每一位取反**：0变1，1变0
5. 左移  ( << )
   **所有位向左移动**，低位补0
6. 右移( >> )
   **所有位向右移动**，高位处理方式不同
7.检查s[i]第i位是否为1
  (S[i] >> j) & 1

## 第一场A（快速幂，逆元，费马小定理）
# 快速幂
计算a^b:
```cpp
long long ksm(long long a, long long b) {
    long long res = 1;
    while (b > 0) {
        if (b & 1) {  // 当前二进制位为1
            res = res * a;
        }
        a = a * a;    // a平方
        b >>= 1;      // b右移一位
    }
    return res;
}
```
### 快速模幂
```cpp
long long ksm_mod(long long a, long long b, long long mod) {
    long long res = 1 % mod;
    a %= mod;
    while (b > 0) {
        if (b & 1) {
            res = (res * a) % mod;
        }
        a = (a * a) % mod;
        b >>= 1;
    }
    return res;
}
int inv100 = ksm(100LL, MOD - 2, MOD);
```



# 模运算
1. 模加法  
```cpp
(a + b) mod MOD = ((a mod MOD) + (b mod MOD)) mod MOD
```
2. 模减法
注意： 需要加 MOD 再取模，防止结果为负数
```cpp
(a - b) mod MOD = ((a mod MOD) - (b mod MOD) + MOD) mod MOD
```
3. 模乘法
和加法相似
```cpp
(a * b) mod MOD = ((a mod MOD) * (b mod MOD)) mod MOD
```
4. 模除法(模运算没有除法，其实是乘上逆元）
### 费马小定理
如果 p 是一个质数，且整数 a 不是 p 的倍数（即 a 与 p 互质），那么：
a^(p-1) % p == 1 % p
也即a在模p的逆元为：
a^(-1) = a^(p-2)%p
代码：
```cpp
const int mod = 998244353;  // 这是一个质数

// 求 a 在模 mod 下的逆元
int inv_a = ksm(a, mod - 2);  // 使用费马小定理

// 在题目中的应用：求 100 的逆元
int inv100 = ksm(100, mod - 2);
```
# .
.
.
### 注意运算顺序，以后还是尽量写
```cpp
t =  t * t % mod;
```
而不是
```cpp
t *= t%mod;
```
因为
t%mod 先计算
t *= (t%mod) 相当于 t = t * (t % mod)
# 数论  
### 最大公约数  
```cpp
int gcd(int a,int b)
{
    return (b==0 ? a :gcd(b,a%b);
}
```
### 二元一次方程求解（扩展欧几里得）  (裴蜀定理：判断方程是否有整数解）例：第四场D题
```cpp
// 使用__int128处理中间计算过程中的大数乘法，防止溢出
using lll=__int128;

// 扩展欧几里得算法：求解方程 a*x + b*y = gcd(a, b) 的一组整数解 (x, y)
// 返回值是 a 和 b 的最大公约数 d
lll exgcd(int a, int b, lll &x, lll &y) {
    if (b==0) { // 递归边界：当 b == 0 时，gcd(a, 0) = a，此时解为 x=1, y=0
        x = 1, y = 0;
        return a;
    }
    // 递归求解：根据欧几里得算法，gcd(a, b) = gcd(b, a % b)
    // 先求方程 b*x' + (a % b)*y' = d 的解
    int x1,y1;
    int d = exgcd(b, a % b, x1, y1); // 注意这里交换了 x 和 y 的位置，是一种技巧
    // 然后回溯，更新解：y = y' - (a/b) * x'
    x = y1;
    y = x1-(a/b)*y1;
    return d;
}
```
# BFS
例题
牛客tracker，走迷宫
```cpp
#include<bits/stdc++.h>
using namespace std;
#define ll long long
#define sc second
#define fr first
#define int long long
#define itn long long
#define vec vector
#define pii pair<int,int>
#define all(a) a.begin(),a.end()
#define ui unordered_map<int,int>
// const int N = 5e5+5;
// const int mod = 998244353;
// const int mod = 1e9+7;
const int N = 1010;
void solve(){
    int n,m;
    cin >> n >> m;
    int xs,ys,xt,yt;
    cin >> xs >> ys >> xt >> yt;
    vec g(n,string(m,'.'));
    int res = 0;
    xs--;ys--;xt--;yt--;//题目索引从1开始，而我这输入的时候是从0开始
    for(auto& i:g){
        cin >> i;
    }
    queue<pii> q;//创建队列，存储待访问
    q.push({xs,ys});//起点入队
    g[xs][ys] = '*';//起点标记为已访问
    while(!q.empty()){
        int s = q.size();//记录这层的元素个数，遍历
        while(s--){
            pii t = q.front();//取出队首，一个个判断
            q.pop();//已经存储到t了，可以踢出
            if(t.first==xt && t.second==yt){//判断是否是终点
                cout << res;
                return;//记得return
            }else {
                vec<pii> dec = {{0,-1},{0,1},{-1,0},{1,0}};//记录下一步
                for(int i=0; i<4; i++){//遍历下一步
                    int nx = t.first + dec[i].first;
                    int ny = t.second + dec[i].second;
                    if(nx>=0 && nx<n && ny>=0 && ny<m && g[nx][ny]=='.'){//符合条件，注意条件范围写全
                        q.push({nx,ny});
                        g[nx][ny] = '*';
                    }
                }
            }
        }
        res++;
    }
    cout << "-1";//前面没有return的话就会启动，说明没有
}
signed main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    int _=1;
    // cin >> _;
    while(_--){
        solve();
    }
}
```
# 数据结构
1.优先队列  (最大堆）（即默认保持队顶最大）
```cpp
priority_queue<int,vec<int>>
```
## 排序  
```cpp
struct Student {
    std::string name;
    int score;
};
```
1.lambda表达式
```cpp
std::vector<Student> students = {{"Alice", 90}, {"Bob", 85}, {"Charlie", 95}};
// 按分数升序排序
std::sort(students.begin(), students.end(),
          [](const Student& a, const Student& b) {
              return a.score < b.score;
          });
```
2.自定义函数
```cpp
bool compareByScore(const Student& a, const Student& b) {
    return a.score < b.score;
}
// 使用函数指针
std::sort(students.begin(), students.end(), compareByScore);
```
前面的大那就大，即这里的a在b前，a大则为降序
## 计算几何
方法：  
1.向量  
2.三角函数  
向量：知道点的坐标  
叉乘法：  
再取一个点，依次用叉乘乘以各个点，就可以得到面积，具体看第三期训练营H题  
三维的话需要xyz的单位方向向量  
# 并查集  
1.路径压缩：将路径节点直接指向根节点  
```cpp
int find(int x) {
    if (parent[x] != x) parent[x] = find(parent[x]);
    return parent[x];
}
```
2.按秩合并：用 rank 数组记录树的高度（或大小），合并时将高度低的树合并到高度高的树上，避免树过高。
```cpp
int rank[10000]; // 初始全0
void unite(int a, int b) {
    int ra = find(a), rb = find(b);
    if (ra == rb) return;
    if (rank[ra] < rank[rb]) parent[ra] = rb;
    else if (rank[ra] > rank[rb]) parent[rb] = ra;
    else { parent[ra] = rb; rank[rb]++; }
}
```
## 埃氏筛
找素数，时间复杂度O(nlog logn)
```cpp
//埃氏筛找素数
vec<bool> isprime;
void sieve(int limit){
    isprime.assign(limit+1,true);//先假设所有元素都是素数
    isprime[0] = isprime[1] = false;//0和1不是
    // 从最小的素数 2 开始，将其倍数全部标记为合数；然后找到下一个未被标记的数（它一定是素数），再将其倍数标记……重复直到 √N
    for(int i=2; i*i<=limit; i++){
        if(isprime[i]){
            for(int j = i*i; j<=limit;j+=i)
                isprime[j] = false;
        }
    }
}
```
## 一些常用stl和坑
1.fixed 常和 setprecision（精度）搭配，比如第三期H题  
  能强制输出小数形式，避免科学计数法的误差  
2.rotate:反转:  
```cpp
std::string s = "HelloWorld";
    std::rotate(s.begin(), s.begin() + 5, s.end());
    std::cout << s << std::endl;  // 输出 "WorldHello"
```
3.insert:  
```cpp
std::vector<int> a = {1, 2, 5, 6};
    std::list<int>   b = {3, 4};
    // 在 a 中指向 5 的位置之前插入 b 的全部元素
    auto it = a.begin() + 2;   // it 指向第三个元素（值为5）
    a.insert(it, b.begin(), b.end());
    // 输出结果: 1 2 3 4 5 6
```
4.sscanf,c_str：
```cpp
sscanf(line.c_str(), "%d and %d are siblings", &x, &y);//字符串形如"5 and 6 are siblings"，根据字符串来匹配整数
//c_str转换成c风格，才能识别
```
5.unsigned long long:  
如果题目要对2^64取模，直接用这个
```cpp
#define int unsigned long long
```
6.true  or  false:  
只有0=false,其他非零都是true;
7.memset:  
用于对数组的初始化，注意是数组，不是vector：  
```cpp
int dp[n];
memset(dp, 0x3f, sizeof(dp));   // 如果 n 是变量，sizeof(dp) 在运行时正确计算（VLA 支持）
// 或者
memset(dp, 0x3f, n * sizeof(long long));  => (数组名，赋值，总字节数)
```
## 卡常
1.减少使用动态分配：  
```cpp
vector<int> v;
v.reserve(n);
```
